services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - ALLOWED_HOSTS=localhost,127.0.0.1
    volumes:
      - sqlite_data:/app/db.sqlite3
    restart: unless-stopped

volumes:
  sqlite_data:
